#include<iostream>
#include<math.h>
using namespace std;

int main(){
	int min == n/2;
	if(n % 2)
		min = n/2 + 1;//neu bac thang la le thi min phai them 1
		//vd 15 bac thi min = 8 buoc	
	int max = n;
	for(int i = min; i <= max; i++){
		if(i % m == 0){
			cout<<i;
			return 0;
		}
	}
	return -1;
}
